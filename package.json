{
  "name": "Word_Race_Server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "engines": {
    "yarn": "1.22.11",
    "node": "14.17.6"
  },
  "scripts": {
    "start": "node app.js",
    "app": "nodemon app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "cd client && yarn install && yarn run build"
  },
  "dependencies": {
    "config": "^3.3.6",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "express-validator": "^6.12.1",
    "mongoose": "^6.0.7",
    "react-typing-game-hook": "^1.3.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}
